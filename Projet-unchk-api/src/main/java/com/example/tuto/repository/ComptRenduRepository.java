package com.example.tuto.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.example.tuto.entities.ComptRendu;

public interface ComptRenduRepository extends JpaRepository<ComptRendu, Long>{
    
}
