package com.example.tuto.enums;

public enum ErrorCode {
        EMAIL_ALREADY_EXISTS,
        INVALID_CREDENTIALS,
        ACCESS_DENIED,
        VALIDATION_ERROR, 
        USER_NOT_FOUND, 
        USER_CREATION_FAILED
    }
